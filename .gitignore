# Rust
/target
messenger-backend/target
src-tauri/target

# Node / frontend
frontend-development/node_modules
frontend-development/.next
frontend-development/out

# Tauri android build output (regenerated by CI)
src-tauri/gen/android/app/build
src-tauri/gen/android/.gradle
src-tauri/gen/android/local.properties

# OS junk
.DS_Store

# gen/android is a generated project — regenerated fresh in CI via `tauri android init`.
# Committing it is unreliable (git doesn't track empty dirs Android/Java expects).
src-tauri/gen/android
